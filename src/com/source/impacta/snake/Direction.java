package com.source.impacta.snake;

public enum Direction {
	
	// 11- Criar a enum de direções, para movimentar a cobra.
	
	/**
	 * Movendo para o norte (para cima).
	 */
	North,
	
	/**
	 * Movendo-se para o leste (direita).
	 */
	East,
	
	/**
	 * Movendo-se para o sul (para baixo).
	 */
	South,
	
	/**
	 * Movendo-se para o oeste (esquerda).
	 */
	West
}
